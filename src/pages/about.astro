---
import { Image } from 'astro:assets';
import BaseLayout from '@/layouts/BaseLayout.astro';
import profileImage from '@/assets/profile/quentin-rospars.jpg';
---

<BaseLayout title="About — Quentin Rospars" description="About Quentin Rospars.">
  <section class="about-page section" aria-labelledby="about-title">
    <div class="container container--wide about-page__grid">
      <header>
        <p class="eyebrow eyebrow--accent">About</p>
        <h1 id="about-title">Hi, I'm Quentin.</h1>
      </header>
      <div class="about-page__portrait">
        <Image
          src={profileImage}
          alt="Portrait of Quentin Rospars"
          widths={[480, 720, 960]}
          sizes="(max-width: 48rem) calc(100vw - 2.5rem), 38vw"
          loading="eager"
          quality={85}
        />
      </div>
      <div class="about-page__copy stack">
        <p>I'm a french computer scientist working as a UX optimisation consultant for <a href="https://ecapacity.com/" rel="noreferrer" target="_blank">eCapacity</a>_<a href="https://www.valtech.com" rel="noreferrer" target="_blank">Valtech</a> in Copenhagen.</p>
        <p>I always have been passionate about Art & Coding, and I try to link these two worlds in my work. My strongest skills are probably data visualization, UX optimization and web development.</p>
        <p>If you think I am someone interesting to talk to, don't hesitate to send me an email <a href="mailto:rospars.quentin@outlook.com">rospars.quentin@outlook.com</a> or follow me on my social media! ❤</p>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .about-page {
    padding-top: clamp(9rem, 18vh, 14rem);
  }

  .about-page__grid {
    display: grid;
    grid-template-columns: repeat(12, minmax(0, 1fr));
    gap: var(--space-6);
    align-items: start;
  }

  header {
    grid-column: 1 / span 8;
    margin-bottom: var(--space-7);
  }

  h1 {
    margin: var(--space-3) 0 0;
    font-size: var(--step-5);
  }

  .about-page__portrait {
    grid-column: 1 / span 5;
    overflow: hidden;
    border-radius: var(--radius-small);
  }

  .about-page__portrait :global(img) {
    aspect-ratio: 4 / 5;
    object-fit: cover;
    filter: saturate(0.75);
  }

  .about-page__copy {
    --stack-space: var(--space-6);
    grid-column: 7 / -1;
    font-size: var(--step-1);
  }

  @media (max-width: 48rem) {
    .about-page__grid {
      grid-template-columns: 1fr;
    }

    header,
    .about-page__portrait,
    .about-page__copy {
      grid-column: 1;
    }
  }
</style>
